# Speedie the Cat 🐱

A Python-based game where Speedie the cat runs, dodges obstacles,
and uses sound effects and graphics.

## How to Run
1. Install Python
2. Install pygame:
   pip install pygame
3. Run:
   python main.py
